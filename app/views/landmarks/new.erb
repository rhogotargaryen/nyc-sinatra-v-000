<h2>Create a new Landmark</h2>
<form method='post' action='/landmarks'>
    <label for='landmark[name]'>Landmark Name</labe>
    <input name='landmark[name]' type='text' id='landmark_name'/></br>
    <label for='landmark[year_completed]'>Landmark Year Created</label>
    <input name='landmark[year_completed]' type='text' id='landmark_year_completed'/></br>
    <h3>Please associate an existing figure with the landmark or create a new figure to associate</h3>
    <% Figure.all.each do |x| %>
    <input type='checkbox' name='landmark[figure_id]' value='<%=x.id%>' id='figure_<%=x.id%>'><%= x.name %></input>
    <% end %>
    </br>
    <h2>Create a new figure and associate</h2>
    <label for='figure[name]'>Figure Name</labe>
    <input name='figure[name]' type='text' id='figure_name'/></br>    
    <input type='submit' value='Create New Landmark'/>
</form>